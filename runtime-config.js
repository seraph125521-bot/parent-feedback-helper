/*
 * Runtime configuration.
 *
 * Vercel keeps this empty and falls back to /api/generate.
 * CloudBase deployment can replace this file in the publish directory with:
 *   window.PFH_CONFIG = { apiEndpoint: "https://..." };
 */
window.PFH_CONFIG = window.PFH_CONFIG || {};
